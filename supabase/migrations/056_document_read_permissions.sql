-- =====================================================
-- セキュリティ決定事項のドキュメント化
-- =====================================================

-- SECURITY DECISION: 広範な読み取り権限は意図的な設計
-- 全認証ユーザーが全員の勤怠・TODO・ランキングデータを閲覧可能
-- これはFDGROUP内のチーム透明性を支援するための設計
-- レビュー日: 2026-02-27
-- チーム規模が50人を超える場合、ロールベースの読み取り制限を検討すること

-- 退職者のアクセス遮断用カラム追加
ALTER TABLE users ADD COLUMN IF NOT EXISTS is_deactivated BOOLEAN DEFAULT false;

-- 退職者のログインを制限するためのRLSポリシー
-- （認証自体はSupabase Authレベルで無効化するが、RLSでも二重チェック）
